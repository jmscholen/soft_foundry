# Deviations

None from the plan agreed with the user (new maturity capability, Golden Signals + RED/USE baseline). The `maturity-report` lifecycle-closure fix was an unplanned but necessary detour, not a deviation from this change's own scope — it was cleanup of a pre-existing gap in a different, already-merged change record, done because it was blocking this change's own commits from passing the local pre-commit hook. It is recorded as its own commit (`78ee8b2`) with its own rationale, not folded silently into this change's substance.
