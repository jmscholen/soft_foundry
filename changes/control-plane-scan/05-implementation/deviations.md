# Deviations From Plan

None. The recommended shape (a content pass in `check` over `.ai/`, `AGENTS.md`, and `CLAUDE.md` for invisible Unicode, override phrases, secret shapes, and fetch-and-execute; errors under policies and warnings elsewhere; the same pass over a record's phase files at gate time) was implemented as recommended, with a policy allowlist added for evidence that cannot be edited once the first run found a canary in a closed record.

Each deviation records: what changed, why the plan could not be followed, which requirements are affected, and who must approve it.
