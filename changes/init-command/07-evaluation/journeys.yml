commit_sha: a7b9df1bd6ac5c50346515d04a9a4a3853b86c6d
previous_runs: "6a41afd123ca (blocked on EVAL-014), c29509c3cee8 (passed), and a733f84086d0 (passed; both invalidated -- the second by a harness fix, the third by the attack-finding remediation), archived under previous/6a41afd123ca/, previous/c29509c3cee8/, and previous/a733f84086d0/"
runtime: "ruby 3.3.1 at /Users/jscholen-iou/.asdf/installs/ruby/3.3.1/bin/ruby; soft-foundry run from source as ruby -I<repo>/lib <repo>/exe/soft-foundry"
journeys:
  - id: EVAL-001
    criteria: [AC-001, AC-003, AC-010]
    persona: first-time maintainer (a)
    starting_state: scratch Git repository with a committed app.rb and no Soft Foundry files
    steps:
      - soft-foundry init --no-onboard
      - inspect tree, manifest, repository.yml, harness-evals, .gitignore, AGENTS.md, CLAUDE.md; recompute every manifest hash; cmp every installed .ai file against the packaged set
      - soft-foundry check; soft-foundry doctor
      - git add -A; git commit; record mtimes; sleep 1
      - soft-foundry init --no-onboard (second run); record mtimes; git status
    assertions:
      - "first run exit 0, report begins with root: <target>, check: ok, summary created 171"
      - app.rb SHA-256 identical before and after
      - manifest has 165 entries, 0 hash mismatches, every on-disk .ai file (except manifest and repository.yml) listed, soft_foundry_version 0.2.0
      - every installed .ai file byte-identical to the packaged source; repository.yml equals the template (assessed false, capabilities {}); harness-evals contains only README.md
      - changes/README.md exists; .gitignore contains .soft-foundry/; both pointer files carry the marker; check passes in the target
      - second run exit 0, only skipped lines, summary created 0 updated 0 conflict 0 forced 0, git status clean, all modification times unchanged (stretch)
    evidence: [evidence/EVAL-001-first-install-idempotent.log, evidence/scripts/eval-001.sh]
    result: pass
  - id: EVAL-002
    criteria: [AC-004, AC-005]
    persona: upgrading maintainer (b)
    starting_state: repository with a committed AGENTS.md holding project rules and a CLAUDE.md without trailing newline, no markers
    steps:
      - soft-foundry init --no-onboard
      - cat both files; cmp the original byte-length prefix; count markers; git diff --stat; commit
      - soft-foundry init --no-onboard (second run); count markers; git status
    assertions:
      - AGENTS.md and CLAUDE.md reported updated (block appended), exit 0
      - original bytes of each file preserved verbatim as the prefix of the new file
      - exactly one soft-foundry:begin marker in each file
      - second run exit 0, both reported skipped (block present), nothing appended, git status clean
    evidence: [evidence/EVAL-002-pointer-files-preserved.log, evidence/scripts/eval-002.sh]
    result: pass
  - id: EVAL-003
    criteria: [AC-012]
    persona: engineering agent (c)
    starting_state: clean scratch repository with a committed main.rs
    steps:
      - soft-foundry init --dry-run --no-onboard
      - inspect tree; confirm no manifest, no .ai/, no .soft-foundry/, git status has no untracked entries
      - soft-foundry init --no-onboard (real run)
      - "diff the dry-run report against the real report (ignoring the mode:/check: lines each only carries)"
    assertions:
      - dry run exit 0, mode line printed, nothing written
      - real run exit 0, report identical to the dry-run preview apart from mode:/check:
      - real run wrote the manifest
    evidence: [evidence/EVAL-003-dry-run-then-real.log, evidence/scripts/eval-003.sh]
    result: pass
  - id: EVAL-004
    criteria: [AC-006, AC-007, AC-003]
    persona: first-time then upgrading maintainer (a)/(b)
    starting_state: repository installed and committed at the current version, then a maintainer commit appending to .ai/rules/general.md
    steps:
      - build a next-version rsync copy of the source whose .ai/rules/general.md and .ai/rules/ruby.md canonical content differs
      - run the next-version soft-foundry init --no-onboard (no --force)
      - commit; run the next-version soft-foundry init --force --no-onboard
      - commit; rerun to confirm the tree is now clean
    assertions:
      - upgrade run exit 3; general.md reported conflict (differs from manifest hash) and left untouched
      - ruby.md (not edited by the maintainer) reported updated and rewritten to the next canonical content
      - "conflict run prints conflicts: naming the path and next: with git diff/--force guidance, both after summary:"
      - "force run prints no conflicts:/next: lines"
      - --force run exit 0; general.md reported forced, content canonical, manifest hash updated
      - "rerun after force: exit 0, all skipped"
    evidence: [evidence/EVAL-004-conflict-then-force.log, evidence/scripts/eval-004.sh]
    result: pass
  - id: EVAL-005
    criteria: [AC-006]
    persona: upgrading maintainer (b)
    starting_state: installed and committed repository, then an uncommitted (not staged, not committed) hand edit to .ai/rules/general.md
    steps:
      - soft-foundry init --no-onboard (no --force)
      - soft-foundry init --force --no-onboard
    assertions:
      - plain run exit 3, conflict reason "uncommitted modifications"
      - --force run still exit 3, still conflict (never forced); the uncommitted edit is intact byte-for-byte
      - "both runs print conflicts: naming the path and next: explaining uncommitted edits are never overwritten"
    evidence: [evidence/EVAL-005-uncommitted-edit.log, evidence/scripts/eval-005.sh]
    result: pass
  - id: EVAL-006
    criteria: [AC-002]
    persona: engineering agent (c)
    starting_state: a plain directory outside any Git work tree (GIT_CEILING_DIRECTORIES pinned to the scratch root)
    steps:
      - soft-foundry init --no-onboard (refused)
      - soft-foundry init --allow-non-git --no-onboard (installs)
      - soft-foundry init --allow-non-git --force --no-onboard (refused, unrecoverable combination)
    assertions:
      - refusal exit 1, message names the missing Git work tree and --allow-non-git, nothing written
      - --allow-non-git installs, exit 0
      - --allow-non-git --force refused as unrecoverable, exit 1
    evidence: [evidence/EVAL-006-non-git.log, evidence/scripts/eval-006.sh]
    result: pass
  - id: EVAL-007
    criteria: [AC-011]
    persona: first-time maintainer (a)
    starting_state: repository with docs/architecture/ but no docs/user/, plus a control repository that already has docs/user/
    steps:
      - soft-foundry init --no-onboard in both repositories
    assertions:
      - exit 0; docs/user/README.md reported skipped with reason "docs/ exists without docs/user/"; not created
      - control repository (docs/user/ already present) gets docs/user/README.md created
    evidence: [evidence/EVAL-007-docs-without-user.log, evidence/scripts/eval-007.sh]
    result: pass
  - id: EVAL-008
    criteria: [AC-009]
    persona: upgrading maintainer (b)
    starting_state: repository whose .gitignore already contains .soft-foundry/ among other entries, plus a control repository whose .gitignore lacks the line and has no trailing newline
    steps:
      - soft-foundry init --no-onboard in both repositories
    assertions:
      - exit 0; .gitignore reported skipped with reason "(.soft-foundry/ is ignored)"; hash and git status unchanged
      - "control repository: line appended on its own line, existing content preserved"
    evidence: [evidence/EVAL-008-gitignore.log, evidence/scripts/eval-008.sh]
    result: pass
  - id: EVAL-009
    criteria: [AC-001, AC-002]
    persona: engineering agent (c)
    starting_state: repository with services/api/ as a subdirectory
    steps:
      - soft-foundry init --root <subdirectory> --no-onboard, invoked from outside the repository (refused)
      - soft-foundry init --dry-run --no-onboard from inside the subdirectory with cwd resolution (no --root)
      - soft-foundry init --root <top-level> --no-onboard, invoked from outside (installs)
      - soft-foundry init --root <nonexistent> --no-onboard
    assertions:
      - subdirectory --root refused, exit 1, message names the top-level, nothing written
      - "cwd inside the subdirectory resolves to the top-level and prints root: <top-level>"
      - top-level --root installs, exit 0, root printed
      - missing --root directory refused, exit 1
    evidence: [evidence/EVAL-009-root-option.log, evidence/scripts/eval-009.sh]
    result: pass
  - id: EVAL-010
    criteria: [AC-015]
    persona: first-time maintainer (a)
    starting_state: clean scratch repository, OPENAI_API_KEY/ANTHROPIC_API_KEY/XAI_API_KEY all unset
    steps:
      - soft-foundry init (onboarding enabled, no --no-onboard)
      - inspect .soft-foundry/runtime.yml, git status, git check-ignore
      - confirm .soft-foundry/ is a plain directory, not a symlink
      - soft-foundry onboard a second time against the same target (legitimate-use re-run)
    assertions:
      - "exit 0; check: ok; manifest present; every provider reported \"not configured (set <ENV_VAR>)\""
      - "runtime.yml exists, records configured: false for all three providers, carries no key-shaped values"
      - runtime.yml is gitignored (no soft-foundry path in git status)
      - ".soft-foundry/ is a plain directory, not a symlink; a second onboard run against it succeeds and still records configured: false for all three"
    evidence: [evidence/EVAL-010-no-provider-keys.log, evidence/scripts/eval-010.sh]
    result: pass
  - id: EVAL-011
    criteria: [AC-016]
    persona: engineering agent (c)
    starting_state: repository producing, in three successive runs, created/updated statuses, then a conflict, then a forced resolution
    steps:
      - capture raw bytes of three runs (plain install, conflicting upgrade, --force)
      - "strip everything outside tab/newline/printable-ASCII with tr; parse status words with awk; cross-check parsed counts against each run's printed summary: line"
    assertions:
      - raw outputs contain no non-ASCII bytes
      - created/updated/skipped/conflict/forced each parseable after stripping, in the run that produces them
      - "parsed counts equal the summary: counts in all three runs"
      - "conflicts:/next: guidance lines identifiable by prefix after stripping, and not mistaken for a per-file conflict status by the parser"
      - exit codes 0, 3, 0 across the three runs
    evidence: [evidence/EVAL-011-ascii-status-words.log, evidence/scripts/eval-011.sh]
    result: pass
  - id: EVAL-012
    criteria: [AC-013]
    persona: first-time maintainer (a) hitting a broken package
    starting_state: an rsync copy of the source with .ai/workflow.yml deleted, invoked against a fresh scratch repository with canary secret values (OPENAI_API_KEY etc.) set in the environment
    steps:
      - run the broken copy's soft-foundry init with stdout/stderr captured separately
      - run the same broken copy with --dry-run --no-onboard
      - scan both streams for the canary secret, the target path, the broken-copy path, and $HOME
    assertions:
      - exit 4; message states the fault is in Soft Foundry, names the missing file and component (package)
      - points to the upstream repository with fork/pull-request guidance
      - nothing written to the target (git status empty, no .ai, no .soft-foundry)
      - no secret value, no absolute target path, no absolute source path, and no home directory path in stdout or stderr
      - guidance goes to stderr; stdout is empty
    evidence: [evidence/EVAL-012-internal-failure.log, evidence/scripts/eval-012.sh]
    result: pass
  - id: EVAL-013
    criteria: [AC-008, AC-010, AC-007]
    persona: pre-manifest maintainer (d)
    starting_state: repository with a hand-copied .ai/ tree (rsync of the packaged set minus harness-evals), two files locally edited, one file deleted, repository.yml hand-edited to assessed true, no manifest
    steps:
      - soft-foundry init --no-onboard
      - inspect manifest scope and content; confirm edited files untouched; commit
      - soft-foundry init --force --no-onboard
    assertions:
      - exit 3 when conflicts exist; identical files skipped (identical); differing files reported conflict (not in manifest); the deleted file recreated (created)
      - summary shows conflict 2; manifest lists only written-or-verified-identical files, no conflict entries
      - conflicting files left untouched by the plain run
      - "repository.yml kept (assessed: true preserved), reported skipped with the ownership reason"
      - "conflicts: line names both conflicted paths; next: line printed once on the conflict run, not on the force run"
      - --force run exit 0; both conflicted files forced to canonical; repository.yml never touched by --force
    evidence: [evidence/EVAL-013-hand-copied-ai.log, evidence/scripts/eval-013.sh]
    result: pass
  - id: EVAL-014
    criteria: ["specification \"Exit codes\""]
    persona: engineering agent (c)
    starting_state: clean scratch repository with a committed app.rb
    steps:
      - soft-foundry (no command, top-level help)
      - soft-foundry init --help
      - soft-foundry init --dry_run --no-onboard (misspelled option)
      - soft-foundry version
      - soft-foundry init --root (no value)
      - soft-foundry init -h; soft-foundry help
    assertions:
      - help lists init with all five documented options, exit 0
      - unknown option refused, exit 1, message names the bad option, nothing written
      - init --help installs nothing
      - version prints a version string, exit 0
      - "--root without a value: exit 1, single-line message \"soft-foundry: --root requires a value\", no defect/upstream/diagnostic text, nothing written"
      - informational probes (-h, help) write nothing
    evidence: [evidence/EVAL-014-cli-surface.log, evidence/scripts/eval-014.sh]
    result: pass
  - id: EVAL-015
    criteria: [AC-014]
    persona: upgrading maintainer (b) with an accidental or hostile layout
    starting_state: repository where .ai is a symlink to a directory outside the repository root
    steps:
      - soft-foundry init --no-onboard
    assertions:
      - exit 1; message names the symlink and states init does not write through symlinks
      - nothing written outside the root (the outside directory stays empty)
      - nothing written inside the root (git status clean)
    evidence: [evidence/EVAL-015-symlinked-ai.log, evidence/scripts/eval-015.sh]
    result: pass
  - id: EVAL-016
    criteria: [AC-012, AC-006]
    persona: engineering agent (c)
    starting_state: installed and committed repository, then a maintainer commit appending to .ai/rules/errors.md so a subsequent upgrade would conflict
    steps:
      - soft-foundry init --dry-run --no-onboard
      - soft-foundry init --dry-run --force --no-onboard
    assertions:
      - "dry run with a conflict exits 3 (same code a real run would return); mode:, conflict, conflicts:, and next: lines all printed"
      - dry run writes nothing; the maintainer's edit is intact afterward
      - dry run with --force shows the would-be forced status, exits 0, and still writes nothing
    evidence: [evidence/EVAL-016-dry-run-conflict-preview.log, evidence/scripts/eval-016.sh]
    result: pass
  - id: EVAL-017
    criteria: [AC-006]
    persona: upgrading maintainer with a build artifact (b)
    starting_state: repository whose .gitignore ignores tmp/, with an untracked tmp/cache build artifact committed-ignored (never tracked), not under .ai/
    steps:
      - soft-foundry init --no-onboard with the ignored artifact present
      - commit the install; make an uncommitted edit to .ai/rules/general.md (a real, in-scope dirty path)
      - soft-foundry init --no-onboard (second run)
    assertions:
      - "first install succeeds (check: ok) with the ignored artifact present; artifact content unchanged; never listed as a managed action"
      - git status after install shows the artifact only as ignored (!!), never untracked or conflicting
      - "second run: only the actually-edited .ai/rules/general.md is a conflict (exit 3, \"uncommitted modifications\"); the build artifact never appears in the conflicts: line"
    evidence: [evidence/EVAL-017-gitignored-build-artifact.log, evidence/scripts/eval-017.sh]
    result: pass
    note: New this run. Legitimate-use companion to the V3 remediation (git.rb#dirty_paths now includes ignored paths); confirms the broadened ignored-path detection is scoped to real .ai/ conflicts and does not treat an unrelated ignored build artifact as relevant.
  - id: EVAL-018
    criteria: []
    persona: upgrading maintainer with a non-conventional layout (b)
    starting_state: repository with source under src/, tests under spec/, infrastructure under infra/ (none matched by the default APP/TESTS/INFRA globs), installed and committed
    steps:
      - edit .ai/repository.yml to add paths -> {APP -> [src/**], TESTS -> [spec/**], INFRA -> [infra/**]}; commit
      - soft-foundry check
      - soft-foundry init --no-onboard (second run, after the override)
      - control step -> add a HARNESS_EVALS override to the same file and run soft-foundry check again
    assertions:
      - the override file is written with all three groups
      - check passes (no error) with the reasonable, non-empty APP/TESTS/INFRA override
      - init stays idempotent after the override -> exit 0, repository.yml still skipped as owned, git status clean
      - control -> overriding the protected HARNESS_EVALS group is still rejected, exit 2, naming the group
    evidence: [evidence/EVAL-018-repository-yml-override.log, evidence/scripts/eval-018.sh]
    result: pass
    note: New this run. Legitimate-use companion to the V4 remediation (protected-group / emptied-override hardening in control_plane.rb and check.rb); confirms a reasonable, non-empty override of an unprotected group still works normally, and is not itself an acceptance criterion (repository.yml path overrides are outside AC-001..017's scope).
  - id: EVAL-019
    criteria: [AC-003, AC-004, AC-005]
    persona: teammate / CI runner cloning an already-installed repository (e)
    starting_state: repository installed and committed (AGENTS.md/CLAUDE.md with the pointer block), then a fresh local git clone of that repository into a separate directory
    steps:
      - shasum and cmp AGENTS.md/CLAUDE.md between the original and the clone
      - soft-foundry init --no-onboard inside the clone
    assertions:
      - the clone reproduces AGENTS.md and CLAUDE.md byte-identical to the original
      - init in the clone exits 0
      - AGENTS.md and CLAUDE.md are both reported skipped (block present), not a false conflict
      - no conflict/updated status anywhere in the fresh-clone run; git status stays clean
    evidence: [evidence/EVAL-019-fresh-clone-block-detection.log, evidence/scripts/eval-019.sh]
    result: pass
    note: New this run. Legitimate-use companion to the agent_files.rb block-detection normalization (the false-conflict family observed in ATTACK-012); confirms block detection holds for the ordinary second-clone case, not only the working tree where the block was first written.
