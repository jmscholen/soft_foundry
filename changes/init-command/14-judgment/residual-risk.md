# Residual Risk

Change: `init-command`. Judged commit: `6bd80f6c419e0a877366a37447f85dc71dd86a30`.

| Risk | Source | Likelihood | Impact | Accepted by | Mitigation owed |
| --- | --- | --- | --- | --- | --- |
| `repository.yml` can redirect `APP`/`TESTS`/`INFRA` to a non-existent path with detection silently defeated, in a code-free or non-conventionally-laid-out repository (`CONTROL_PLANE`/`HARNESS_EVALS` remain unconditionally protected regardless). | ATTACK-F-N01 / REV-005, `08-attack/handoff.yml`, `13-review/security.md` | Low — requires the repository's own maintainer/committer to pre-place the override before first `init`/`check`; not a remote or external attacker vector. | Medium — reduced staleness/permission-drift detection quality for three path groups in a narrow repository shape; no data loss, credential exposure, or write-outside-root. | Review (`13-review/consolidated.md`, `security.md`), carried to this judgment | Follow-up change to `Check#check_path_groups`: drop the `!defaults.empty?` condition so a zero-match override is flagged regardless of whether the untouched default also matches nothing. |
| `Installer#plan`'s `clean` computation covers the entire plan (including ordinary `.gitignore`/`AGENTS.md` adapter actions), so a genuine Soft-Foundry-side defect surfaced by the post-install check on a non-empty target is misreported as target-side (exit 0 with "resolve the conflicts above") rather than upstream (exit 4). Fails only in the safe direction — cannot cause a false accusation against the user's repository. | ATTACK-F-N02 / REV-006, `08-attack/handoff.yml`, `13-review/security.md` | Medium — the "adopting into an existing repository with a pre-existing `.gitignore`/`AGENTS.md`" case is the common case, not an edge case. | Low — worse upstream bug-report experience, not a security or data-integrity issue; functional, not security, per attack and review's independent concurrence. | Review, carried to this judgment | Follow-up change: scope `plan.clean` to `.ai/`-kind actions only, excluding `.gitignore`/`AGENTS.md`/`CLAUDE.md` adapter actions. |
| MIT-008: `CLI#resolve_root`'s `--allow-non-git` branch returns the candidate root without a `File.realpath` call, so a `--root` argument that is a symlink to `/` or `$HOME` is only string-compared to the literal `/`, not realpath-verified, unlike the normal (`--allow-non-git` absent) resolution path. | `03-threat-model/threats.yml` (THREAT-008), carried unremediated through `09-remediation/summary.md` (REM-005), `06-verification` (VER carried), `08-attack` (ATTACK-F-009), `13-review/security.md` (REV-004) | Low — requires the invoking user to pass `--allow-non-git` together with an attacker- or mistake-planted symlinked `--root`; not remotely exploitable. | Medium if triggered — could direct writes at `/` or `$HOME` outside the intended repository. | Threat model (accepted at threat-modeling), reconfirmed unaddressed through remediation and review, carried to this judgment | Add `File.realpath` verification (or an explicit refusal) to the `--allow-non-git` branch of `resolve_root`. |
| MIT-014: no streaming/size bound on managed-file or manifest reads; whole files are loaded into memory (peak RSS roughly 2x the largest file observed: 1.09 GB for a 512 MiB file). No hang or crash observed at tested sizes, but no cap exists. | `03-threat-model/mitigations.md` (MIT-014), `08-attack/handoff.yml` (ATTACK-F-010), `09-remediation/summary.md` (deliberately not addressed) | Low — requires an attacker- or mistake-supplied very large file in the packaged set or target repository; local trust boundary. | Low-Medium — potential memory-pressure denial of service on very large inputs, not data exposure or write-outside-root. | Threat model, remediation (deliberately deferred), review, carried to this judgment | Streaming hash / size cap for packaged and manifest reads, per MIT-014's original design intent. |
| Automated-test coverage for the `updated` status path (a managed `.ai/` file whose manifest hash matches but whose canonical content has changed — the upgrade path) is thin: exercised only for `AGENTS.md`/`CLAUDE.md` end-to-end, no dedicated automated test for the general case. | `06-verification/handoff.yml` VER-001 (carried across all 4 verification runs) | N/A (coverage gap, not an exploit) | Low — functional risk that a future regression in the upgrade path goes undetected by CI; end-to-end evidence exists, automated regression coverage does not. | Verification (carried), review (not separately re-flagged as blocking), carried to this judgment | Add a dedicated automated test for the general `updated`-status upgrade path (the MIT-003 test referenced in the plan but never written). |
| `init --help`/`-h` and `soft-foundry help` do not provide per-command help; `--help`/`-h` are refused as "unknown option(s)" (exit 1), and `help` prints only the global usage before exiting 1. | `07-evaluation` EVAL-F-002 (carried from evaluation run 1), `09-remediation/summary.md` REM-002/REM-005 (deliberately not addressed), `06-verification` VER-009, `13-review` (accepted, not re-litigated) | N/A (known, permanent absence, not probabilistic) | Low — discoverability/usability gap for new users of a filesystem-writing CLI command; no functional or security impact. | Remediation (deliberately deferred, scope left to review), review (accepted), carried to this judgment | Decide in a follow-up change whether per-command `--help` is in scope; implement if so. |
| Conflict guidance text is generic: the `next:` line is a single, non-path-specific sentence (commit first / keep the file / `--force` and rerun); the operative step on an uncommitted-edit conflict is only implied by a parenthetical, and the line can wrap on narrow terminals. | `07-evaluation` EVAL-F-003 (carried across all 4 evaluation runs), `06-verification` VER-006 (git-diff hint for `updated`/`forced` files still absent on non-conflict runs) | N/A (known, permanent, not probabilistic) | Low — usability gap; `conflicts:` correctly names every conflicted path, and `next:` is present and not misleading, just generic. | Remediation (partially addressed: `conflicts:`/`next:` lines were added by the first remediation run; further specificity deliberately not pursued), review, carried to this judgment | Follow-up change to make `next:` guidance path- and case-specific, and add the `git diff` hint after `updated`/`forced` on non-conflict runs. |
| Running `soft-foundry` from source (not via `gem install`, which enforces `required_ruby_version >= 3.2` at install time) under Ruby 2.7 fails with a raw `SyntaxError` on endless-method syntax in `cli.rb`, instead of a clear version-guard message. | `06-verification` VER-004 (carried across all runs, reproduced live at HEAD 147fca6, evidence `e2e-install-ruby27-attempt.log`) | Low — only affects source/bundler users who bypass the gem's own version floor enforcement. | Low — confusing error message, not a security or data-integrity issue; the gem-install path is correctly guarded. | Verification (carried), review (not separately re-flagged as blocking), carried to this judgment | Add an explicit Ruby-version guard at the top of the executable entrypoint (`exe/soft-foundry`) that runs before any Ruby-3.2-only syntax is parsed, if source/bundler use below the floor is meant to be supported gracefully. |
| No user-facing prose documentation for `init` beyond a README pointer to the change record: flags, exit codes (0/1/3/4), the `conflicts:`/`next:`/`summary:` report vocabulary, the five status words, `doctor`'s manifest check, and `repository.yml` path overrides are undocumented for end users. | `05-implementation/deviations.md` (originally a gap), `13-review/consolidated.md` deviation (c) (accepted, superseded by explicit maintainer scope decision), `VER-007` (major, carried), `EVAL-F-006` | N/A (known, deliberate, permanent absence for this change) | Medium — a security-sensitive, filesystem-writing command shipped with no user-facing documentation of its exit codes, conflict-resolution flow, or `--force` semantics; mitigated by the CLI's own self-describing output (`help`, `doctor`) but not equivalent to prose documentation. | Maintainer (explicit scope decision, recorded in `13-review/consolidated.md` and reflected in `.ai/workflow.yml`'s `optional: true` on `document`/`index`), reconfirmed by review, carried to this judgment | A future change should run `10-user-documentation` (and `11-faq-index` if warranted) to document `init`'s full operational surface for end users. |
| Structural governance gap: no skill currently holds write permission for `.ai/templates/repository.yml` (required by `Installer::Source::REQUIRED`) or for wrapping `AGENTS.md` in begin/end markers (required by `Installer::Source#agents_interior`), both of which are structurally necessary for this change to function and be dogfoodable in this repository. Both writes were made as documented deviations and approved by review with rationale, but the underlying permission model has no lane for "packaged content that is also self-hosted." | `05-implementation/deviations.md` (a)/(b), `13-review/architecture.md`, `13-review/consolidated.md` (deviation rulings), REV-007/REV-008 | N/A (governance/process risk, not a runtime risk) | Low-Medium — every future change touching these two self-referential files will repeat the same deviation-and-review cycle until the permission model is amended. | Review (approved with rationale for this change), recommended to `15-learning`, carried to this judgment | Amend repository-discovery's `permissions.yml` (or `.ai/README.md`'s stated invariant) to explicitly authorize `.ai/templates/repository.yml`, and define a narrow write lane for self-referential packaged files (`AGENTS.md`/`CLAUDE.md` markers). |

## Not carried as residual risk (accounted for, not defects)
- `10-user-documentation`, `11-faq-index`, `12-observability` being `pending`: deliberate, disclosed maintainer scope decision, now modeled as `optional: true` in `.ai/workflow.yml`. The documentation *absence* is captured above as a residual-risk row (its user-facing consequence); the phase-skip itself is not a process defect.
- ATTACK-F-013/F-014, ATTACK-F-N03: confirmed-safe/controls-held findings from the attack phase; no residual risk attaches.
- REV-001, REV-002, REV-003: informational-only review observations (glyph inconsistency outside `init`'s scope, `verified_by` traceability convenience gap, un-exploitable root-ancestry re-verification gap) — read in full in `13-review/*.md`; none rises to a risk a maintainer needs to consciously accept beyond what is already listed above.
