commit_sha: 52971d8f2fbcad692403709c8f9368c51b5c2eaa
ruby: ruby 3.3.1 (2024-04-23 revision c56cd86388) [arm64-darwin23]
rerun_of: ca3cf40bcd299e98bfd821c3cc9c0d70e79e27ad  # archived under previous/ca3cf40bcd29/; the first run 6df4d15e73d8 is under previous/6df4d15e73d8/
rerun_reason: harness change only (gate loop semantics; lib/soft_foundry/check.rb, control_plane.rb, gate.rb, test/check_test.rb, test/gate_test.rb); init product code identical to ca3cf40
checks:
  - id: CHECK-001
    kind: custom
    command: git rev-parse HEAD && git status --porcelain
    result: pass
    evidence: evidence/git-state.log
    criteria: []
    notes: HEAD 52971d8 on change/init-command. Worktree clean at phase start (2026-09-09T20:47:58Z); at capture time the only modified path is this phase's own git-state.log, and `git status --porcelain -- . ':!changes/init-command/06-verification'` is empty.
  - id: CHECK-002
    kind: custom
    command: which ruby && ruby -v && ruby -e 'puts RbConfig.ruby'
    result: pass
    evidence: evidence/ruby-version.log
    criteria: []
    notes: asdf shim resolving to /Users/jscholen-iou/.asdf/installs/ruby/3.3.1/bin/ruby; the same absolute binary was used for every scratch-directory run.
  - id: CHECK-003
    kind: tests
    command: bundle exec rake test
    result: pass
    evidence: evidence/tests.log
    criteria: [AC-001, AC-002, AC-003, AC-004, AC-005, AC-006, AC-007, AC-008, AC-009, AC-010, AC-011, AC-012, AC-013, AC-014, AC-015, AC-016, AC-017]
    notes: 74 runs, 761 assertions, 0 failures, 0 errors, 0 skips (previous run 71/755; gate_test.rb replaced one remediation-loop test with three and check_test.rb added one for bad `after` targets). Zero warnings in the log. Bundler was available; the fallback runner was not needed.
  - id: CHECK-004
    kind: lint
    command: for f in $(find lib -name '*.rb' | sort); do ruby -wc $f; done
    result: pass
    evidence: evidence/ruby-wc.log
    criteria: []
    notes: 18 files, 18 x Syntax OK, zero warnings, every exit 0.
  - id: CHECK-005
    kind: custom
    command: ruby -Ilib exe/soft-foundry check
    result: pass
    evidence: evidence/check.log
    criteria: [AC-001]
    notes: Exit 0; 16 phases, 16 skills, no errors (includes the new `after` target validation).
  - id: CHECK-006
    kind: custom
    command: ruby -Ilib exe/soft-foundry ci
    result: pass
    evidence: evidence/ci.log
    criteria: []
    notes: "Exit 0. Phases 00-05 PASS at their recorded SHAs; 06-verification and 07-evaluation pending (skipped, not listed); 09-remediation PASS with `predecessor complete: 05-implementation`, the effective-predecessor rule from the workflow's `after: implement`; upstream-failure-reporting 00-intake PASS."
  - id: CHECK-007
    kind: custom
    command: ruby -Ilib exe/soft-foundry doctor
    result: pass
    evidence: evidence/doctor.log
    criteria: []
    notes: Exit 2, expected on this repository. `.ai/manifest.yml` absent (self-install not performed by implementation) and `local runtime` absent; the pre-commit hook is present.
  - id: CHECK-008
    kind: custom
    command: ruby -Ilib exe/soft-foundry init --dry-run --no-onboard
    result: pass
    evidence: evidence/init-dry-run-self.log
    criteria: [AC-012]
    notes: "Exit 0; `mode: dry run, nothing written`; 170 skipped, CLAUDE.md the only `created`; neither .ai/manifest.yml nor CLAUDE.md exists afterwards; nothing outside this phase directory changed."
  - id: CHECK-009
    kind: custom
    command: gem build soft_foundry.gemspec --output <scratch>/soft_foundry.gem (in an rsync copy of the repository); tar -xOf <gem> data.tar.gz | tar -tz; metadata.gz; gem specification <gem> required_ruby_version
    result: pass
    evidence: evidence/gem-build.log
    criteria: [AC-017]
    notes: Built soft_foundry 0.2.0 with 189 files; changes/README.md, docs/user/README.md, .ai/templates/repository.yml, .ai/templates/handoff.yml, .ai/workflow.yml, exe/soft-foundry, AGENTS.md present; .ai/harness-evals/README.md is the only harness-evals entry; .ai/manifest.yml, test/, and changes/init-command not packaged; required_ruby_version >= 3.2 confirmed from metadata.gz and gem specification. No .gem left in the repository. RubyGems warned about missing license and homepage.
  - id: CHECK-010
    kind: custom
    command: scratch git repository with app.rb, absolute ruby 3.3.1; init --no-onboard (twice); check; doctor; edit .ai/rules/general.md and commit; init (conflict); init --force; init
    result: pass
    evidence: evidence/e2e-install.log
    criteria: [AC-001, AC-003, AC-006, AC-007, AC-010, AC-016]
    notes: "Exit codes 0 / 0 / 0 / 2 / 3 / 0 / 0. First init 171 created, `check: ok`, app.rb SHA-256 unchanged, git status clean after commit; second init 171 skipped and no created/updated/conflict/forced line; check exit 0; doctor exit 2 (manifest present, hook and runtime absent because --no-onboard); conflict run exit 3 with `conflict  .ai/rules/general.md  (differs from manifest hash)`, `conflicts: .ai/rules/general.md`, and a `next:` line naming --force, worktree untouched; --force exit 0 with `forced` line and no conflicts/next line, git diff --stat shows the 3 maintainer lines removed, on-disk SHA-256 equals the manifest entry; final run 171 skipped."
  - id: CHECK-011
    kind: custom
    command: ruby verify_manifest.rb <e2e target> (YAML safe_load without aliases, compare against every file under .ai/, recompute SHA-256; script source appended to the log)
    result: pass
    evidence: evidence/e2e-manifest-verify.log
    criteria: [AC-001, AC-007]
    notes: version 1, soft_foundry_version 0.2.0; 165 entries covering every .ai/ file except .ai/repository.yml (excluded by rule); 0 hash mismatches, 0 malformed entries, no path outside .ai/, after --force.
  - id: CHECK-012
    kind: custom
    command: "scratch git repository, absolute ruby 3.3.1: init --root (no value); install, commit edits to .ai/rules/general.md and .ai/rules/ruby.md, init (conflict); init --force; env -u OPENAI_API_KEY -u ANTHROPIC_API_KEY -u XAI_API_KEY init (with onboarding)"
    result: pass
    evidence: evidence/remediation-checks.log
    criteria: [AC-015, AC-016]
    notes: "Remediation-specific. R1 (EVAL-F-001): exit 1, output is the single line `soft-foundry: --root requires a value`; zero matches for `defect in Soft Foundry`, `internal failure`, or pull-request guidance; nothing written to the target. R2 (EVAL-F-003/F-004): exit 3; `conflicts: .ai/rules/general.md, .ai/rules/ruby.md`; `next: compare with `git diff`, keep your version, or commit it and rerun with --force to take the canonical version (uncommitted edits are never overwritten)`; status words survive stripping to printable ASCII (169 skipped; the 3 `conflict` count includes the `conflicts:` summary line matched by this run's regex, a script artifact); --force then exit 0 with 2 forced. R3 (EVAL-F-005 / AC-015): exit 0; `openai     not configured (set OPENAI_API_KEY)`, `anthropic  not configured (set ANTHROPIC_API_KEY)`, `xai        not configured (set XAI_API_KEY)`; .soft-foundry/runtime.yml written with configured: false for all three and ignored via .gitignore:1."
  - id: CHECK-013
    kind: custom
    command: ruby -Ilib exe/soft-foundry gate remediate; ruby -Ilib exe/soft-foundry gate remediate --change init-command; ruby -Ilib exe/soft-foundry change status init-command
    result: pass
    evidence: evidence/gate-remediate.log
    criteria: []
    notes: "Harness-specific check for this rerun. Both gate invocations exit 0 with `09-remediation  complete  PASS` and `predecessor complete: 05-implementation`; ci (CHECK-006) reports the same. change status shows 00-05 and 09 complete/ok, 06-08 and 10-15 pending. workflow.yml carries `optional: true` and `after: implement` on the remediate entry."
  - id: CHECK-014
    kind: custom
    command: ruby -Ilib exe/soft-foundry init --help; ruby -Ilib exe/soft-foundry init -h; ruby -Ilib exe/soft-foundry help
    result: fail
    evidence: evidence/init-help-probe.log
    criteria: []
    notes: "Not a required gate; probe for the carried EVAL-F-002 / REM-002. `init --help` and `init -h` exit 1 with `soft-foundry: unknown option(s): --help`; the global usage lists init and its five options, but `soft-foundry help` itself exits 1 after printing it. Recorded as VER-009 for review, per remediation's deferral."
  - id: CHECK-015
    kind: custom
    command: /Users/jscholen-iou/.asdf/installs/ruby/2.7.8/bin/ruby -Ilib exe/soft-foundry version; same binary, init --dry-run --no-onboard
    result: error
    evidence: evidence/e2e-install-ruby27-attempt.log
    criteria: []
    notes: Environment probe below the declared Ruby floor, reproduced deliberately at this commit. SyntaxError on endless method definitions at lib/soft_foundry/cli.rb lines 63, 140, 141; exit 1; no version guidance. gem install would refuse via required_ruby_version. Carried as VER-004.
