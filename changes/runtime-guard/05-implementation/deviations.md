# Deviations From Plan

None. The recommended shape (a `guard` command reading the active skill from the record, `hooks install --claude` writing the PreToolUse entry, a policy with warn and block modes plus a machine-local override, doctor reporting the hook, the maturity capability moving to PASS) was implemented as recommended.

Each deviation records: what changed, why the plan could not be followed, which requirements are affected, and who must approve it.
