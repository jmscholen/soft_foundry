# Deviations From Plan

None. The two items the maintainer asked for (Grok in the runner's shell table; a Codex hook install with whatever adapter Codex's contract needs, checked against a real install) were implemented as asked; the "adapter" turned out to be a patch parser and array handling inside the guard rather than a separate translator, because that is what Codex's contract required.

Each deviation records: what changed, why the plan could not be followed, which requirements are affected, and who must approve it.
