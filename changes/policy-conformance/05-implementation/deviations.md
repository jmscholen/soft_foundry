# Deviations From Plan

None. The recommended shape (a rule file, `policies:` in the repository profile, a surface flag, a `category: policy` requirement, a review file, `awaiting_human` for owed policy text, a level-5 capability) was implemented as recommended, with one naming change recorded in `decisions.md` (`surfaces.policy` rather than `surfaces.personal_data`).

Each deviation records: what changed, why the plan could not be followed, which requirements are affected, and who must approve it.
