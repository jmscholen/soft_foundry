# Deviations From Plan

None. The recommended shape (two named tracks in `workflow.yml` sharing one gate, a repository default with a per-change override, risk forcing the track, an exploring status with a stage skill and a journal, `change vet` locking the specification, `change reopen` back to exploring, environments in the repository profile, a human-boundary line) was implemented as agreed, with `gated` as this repository's default and `iterative` opt-in.

Each deviation records: what changed, why the plan could not be followed, which requirements are affected, and who must approve it.
