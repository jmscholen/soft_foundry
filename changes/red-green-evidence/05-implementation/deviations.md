# Deviations From Plan

None. The recommended shape (optional `red_commit` and `test_path` per verification check, a gate check verifying the commits' shape against git, an advisory for features verified without one, the testing rule and skill text) was implemented as recommended, and the change itself was done test-first as its own first example.

Each deviation records: what changed, why the plan could not be followed, which requirements are affected, and who must approve it.
