# Deviations From Plan

None. The recommended shape (an `instincts.yml` per record with id, trigger, action, confidence, and evidence; a `learn promote` that copies those above a threshold into `.ai/rules/learned.md` through a change record; the file as a baseline rule) was implemented as recommended, and the change completed its own learning phase as the first example.

Each deviation records: what changed, why the plan could not be followed, which requirements are affected, and who must approve it.
