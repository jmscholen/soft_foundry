# Deviations

None from the plan agreed with the user (an automated `ci` stop-gap for the mechanical half, plus a PR-comment-gated `change close`/`request-discharge` for the human-verification half). The `commit_sha_at_judgment` fix (see 05-implementation/log.md) is not a deviation from scope — it's the implementation correctly self-correcting a bug found by testing against real data (this repository's own change records) before it shipped, exactly the kind of thing verification is supposed to catch.
