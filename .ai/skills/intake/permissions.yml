read:
  - "AGENTS.md"
  - "${CONTROL_PLANE}"
  - "${DOCS}"
write:
  - "changes/${CHANGE}/00-intake/**"
deny_read:
  - "${HARNESS_EVALS}"
deny_write:
  - "${APP}"
  - "${TESTS}"
  - "${INFRA}"
  - "${DOCS}"
  - "${CONTROL_PLANE}"
