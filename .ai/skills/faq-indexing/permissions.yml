read:
  - "${DOCS}"
  - "${CONTROL_PLANE}"
  - "changes/${CHANGE}/10-user-documentation/**"
write:
  - "changes/${CHANGE}/11-faq-index/**"
  - "docs/user/faq/**"
deny_read:
  - "${HARNESS_EVALS}"
deny_write:
  - "${APP}"
  - "${TESTS}"
  - "${INFRA}"
  - "${CONTROL_PLANE}"
