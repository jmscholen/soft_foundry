read:
  - "${CONTROL_PLANE}"
  - "changes/${CHANGE}/**"
write:
  - "changes/${CHANGE}/15-learning/**"
deny_read:
  - "${HARNESS_EVALS}"
deny_write:
  - "${APP}"
  - "${TESTS}"
  - "${INFRA}"
  - "${DOCS}"
  - "${CONTROL_PLANE}"
